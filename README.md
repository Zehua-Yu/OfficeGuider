# OfficeGuider
Turn office action sequences into an ordering problem—greedy decoding on 2D embeddings for recommended execution order. Stack: FastAPI + PyTorch inference, React + ECharts UI. Features: pretrained dir, training jobs, custom atoms, OpenAI-compatible agent proxy. VON vendored under third_party/. MIT license.
